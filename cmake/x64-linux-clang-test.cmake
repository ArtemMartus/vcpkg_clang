set(VCPKG_TARGET_ARCHITECTURE x64)
set(VCPKG_CRT_LINKAGE static)
set(VCPKG_LIBRARY_LINKAGE static)

set(VCPKG_CHAINLOAD_TOOLCHAIN_FILE "${CMAKE_CURRENT_LIST_DIR}/x64-linux-clang/x64-linux-clang.toolchain.cmake")

# set(VCPKG_POLICY_SKIP_ARCHITECTURE_CHECK enabled)
# set(VCPKG_POLICY_SKIP_DUMPBIN_CHECKS enabled)

set(VCPKG_CMAKE_CONFIGURE_OPTIONS 
    )

message("x64-linux-clang-test triplet")

if(DEFINED CURRENT_PORT_DIR AND 
   DEFINED CURRENT_PACKAGES_DIR AND 
   DEFINED CURRENT_BUILDTREES_DIR AND
   DEFINED TARGET_TRIPLET AND
   DEFINED TARGET_TRIPLET_FILE AND
   DEFINED VCPKG_BASE_VERSION AND
   DEFINED VCPKG_MANIFEST_INSTALL AND
   DEFINED CMD)
   message("inside vcpkg")
else()
    message("not in vcpkg")
endif()

list(APPEND VCPKG_CMAKE_CONFIGURE_OPTIONS 
            "-DCMAKE_TRY_COMPILE_CONFIGURATION=Release"
            "-DVCPKG_USE_LTO:BOOL=TRUE"
)
